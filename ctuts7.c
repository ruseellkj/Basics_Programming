#include <stdio.h>

int main()
{
    /* code */
    int a, b;
    a = 37;
    b = 7;

    printf("a + b is %d\n", a + b);
    printf("a - b is %d\n", a - b);
    printf("a * b is %d\n", a * b);
    printf("a / b is %d\n", a / b);

    return 0;
}

